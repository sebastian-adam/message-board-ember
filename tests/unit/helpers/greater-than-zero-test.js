import { greaterThanZero } from 'message-board-ember/helpers/greater-than-zero';
import { module, test } from 'qunit';

module('Unit | Helper | greater than zero');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = greaterThanZero([42]);
  assert.ok(result);
});
